{
  "name": "express_crude",
  "version": "1.0.0",
  "description": "Express app that implements a server that will support CRUD operations of users as reset API server.",
  "main": "app.mjs",
  "scripts": {
    "dev": "nodemon -r dotenv/config app2.mjs",
    "dev:router": "nodemon -r dotenv/config router.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adi Booker",
  "license": "ISC",
  "dependencies": {
    "@ajar/marker": "^1.2.3",
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "uuid": "^9.0.1",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
